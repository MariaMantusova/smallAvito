import React, {useState} from "react";
import {IPropsFormChange} from "../../interfaces/interfacesForProps";
import {useInput} from "../../hooks/ValidationHook/ValidationHook";
import Form from "./Form";

function FormChangeAvto(props: IPropsFormChange) {
    // return(
    //     <Form onSubmit={props.onSubmit} />
    // )
}

export default FormChangeAvto;